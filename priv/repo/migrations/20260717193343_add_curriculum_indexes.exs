defmodule Matwork.Repo.Migrations.AddCurriculumIndexes do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create index(:lessons, [:gym_id, :section_id])

    create index(:lessons, [:gym_id])

    create index(:courses, [:gym_id])

    create index(:course_sections, [:gym_id, :course_id])

    create index(:course_sections, [:gym_id])
  end

  def down do
    drop_if_exists index(:course_sections, [:gym_id])

    drop_if_exists index(:course_sections, [:gym_id, :course_id])

    drop_if_exists index(:courses, [:gym_id])

    drop_if_exists index(:lessons, [:gym_id])

    drop_if_exists index(:lessons, [:gym_id, :section_id])
  end
end
